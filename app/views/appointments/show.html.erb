<div class="container">
  <div class="well">

    <div class="page-header">

      <p id="notice"><%= notice %></p>
      <h1>review your Appointment</h1>
      <p class="lead">
        Department:<strong><%= @appointment.department.name %></strong>
      </p>

      <p class="lead">
        Doctor:<%= @appointment.doctor.name %>
      </p>

      <p class="lead">
        Date of appointment:<%= @appointment.date_of_appointment.strftime("%d/%m/%y" ) %>
      </p>

      <p class="lead">
        Time of appointment:<%= @appointment.time_of_appointment %>
      </p>
  </div>


    <%= link_to 'Edit', edit_appointment_path(@appointment),:class => 'btn btn-danger btn-lg' %>
    <%= link_to 'Back', appointments_path ,:class => 'btn btn-primary btn-lg'%>

  </div>

</div>


